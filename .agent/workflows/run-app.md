---
description: Start the Generative Market app and open it in the browser
---
// turbo-all
1. Check if dependencies are installed
```powershell
npm install
```

2. Start the development server
```powershell
npm run dev
```

3. Open the application in the browser (usually http://localhost:5173)
